# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'fraise' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftySound'

  # Pods for fraise

  target 'fraiseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fraiseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end